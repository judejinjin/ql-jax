# Benchmarks

CPU vs GPU JIT benchmark suite for QL-JAX. See `IMPLEMENTATION_PLAN.md` Phase 8 for details.

## Usage

```bash
python runner.py --backend cpu --size medium --precision float64
```

Results are written to `results/`.
